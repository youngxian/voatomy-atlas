import type { ProductKey } from './activation';

export const PRODUCT_LABELS: Record<ProductKey, string> = {
  atlas: 'ATLAS',
  loop: 'LOOP',
  signal: 'SIGNAL',
  drift: 'DRIFT',
  phantom: 'PHANTOM',
  nexus: 'NEXUS',
};
